{
  "name": "example-basic",
  "version": "0.1.0",
  "description": "Basic PrisML example: user lifetime value prediction",
  "private": true,
  "scripts": {
    "dev": "tsx src/index.ts",
    "generate": "prisma generate",
    "db:push": "prisma db push --skip-generate",
    "seed": "tsx prisma/seed.ts",
    "setup-demo": "pnpm run db:push && pnpm run generate && pnpm run seed",
    "train": "prisml train --config ./prisml.config.ts --schema ./prisma/schema.prisma",
    "train:demo": "prisml train --config ./prisml.config.ts --schema ./prisma/schema.prisma --output ../../apps/website/demo-artifacts",
    "infer": "tsx src/infer.ts"
  },
  "dependencies": {
    "@vncsleal/prisml": "workspace:*",
    "@prisma/client": "^5.7.0",
    "tsx": "^4.7.0"
  },
  "devDependencies": {
    "@vncsleal/prisml-cli": "workspace:*",
    "prisma": "^5.7.0"
  }
}
